# 飞书 SDK（支持 WebSocket 长连接 + event.callback 卡片回调）
# 1.3.5 缺 lark_oapi.event.callback.model.p2_card_action_trigger → 朋友 install 死循环
# 1.6.1 yanked (webhook signature 回归) → 用 1.6.4
lark-oapi==1.6.4

# 抖音下载（内置签名，解决抖音反爬）
# 注意：需要 Python 3.10+（douyin-downloader 使用了 3.10+ 语法）
# ⚠️ 朋友机器（中国大陆）DNS 解析 github.com 失败 → git clone 必挂 → install rollback
#    暂时注释，朋友想用抖音下载时手工:
#    pip install --user git+https://github.com/jiji262/douyin-downloader.git@0109645fe07d3fc0354920ee2ec546bc709a9020
# git+https://github.com/jiji262/douyin-downloader.git@0109645fe07d3fc0354920ee2ec546bc709a9020
# yt-dlp 用于 cookies 提取（DOUYIN_COOKIES_BROWSER 模式）
yt-dlp>=2025.1.1
aiohttp>=3.9.0

# 阿里百炼（ASR 语音转文字 + Qwen 改写）
dashscope==1.19.0

# Claude API（改写）
anthropic>=0.25.0

# Mac 本地 claude_server.py HTTP 服务
flask>=3.0

# 配置 & 工具
python-dotenv==1.0.1
pyyaml==6.0.1
loguru==0.7.2
json-repair>=0.3.0
